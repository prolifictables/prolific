services:
  - type: web
    name: prolific-api
    runtime: node
    plan: free
    region: oregon

    # Render defaults to latest Node (e.g. 26.x) which breaks native modules.
    # Pin to Active LTS v20 — better-sqlite3 (used by POS only, not server)
    # has prebuilt binaries for v20 so no native compile is triggered.
    # Also note: the server workspace does NOT even use better-sqlite3; it is
    # pulled in only by the POS/Electron workspaces via monorepo npm ci.
    buildCommand: npm ci --ignore-scripts && npx turbo run build --filter=@prolific/server...
    startCommand: npm run start:prod --workspace=@prolific/server

    healthCheckPath: /api/v1

    buildFilter:
      paths:
        - server/**
        - packages/**
        - package.json
        - package-lock.json
        - turbo.json
        - render.yaml

    envVars:
      - key: NODE_VERSION
        value: "20.17.0"

      - key: NODE_ENV
        value: production

      - key: MONGODB_URI
        sync: false

      - key: JWT_ACCESS_SECRET
        sync: false

      - key: JWT_REFRESH_SECRET
        sync: false

      - key: JWT_ACCESS_TTL_SECONDS
        value: "1800"

      - key: JWT_REFRESH_TTL_SECONDS
        value: "604800"

      - key: JWT_ISSUER
        value: prolific.pos

      - key: JWT_AUDIENCE
        value: prolific.clients

      - key: CORS_ORIGIN
        value: https://prolifictables.com

      - key: DEFAULT_PAYMENT_PROVIDER
        value: PAYSTACK

      - key: PAYSTACK_SECRET_KEY
        sync: false

      - key: PAYSTACK_PUBLIC_KEY
        sync: false

      - key: PAYSTACK_WEBHOOK_SECRET
        sync: false

      - key: FLUTTERWAVE_SECRET_KEY
        sync: false

      - key: FLUTTERWAVE_PUBLIC_KEY
        sync: false

      - key: FLUTTERWAVE_WEBHOOK_SECRET
        sync: false

      - key: SEED_ENABLED
        value: "true"

      - key: SEED_RUN_ONCE
        value: "true"

      - key: SEED_SUPERADMIN_EMAIL
        value: superadmin@prolific.ai

      - key: SEED_SUPERADMIN_PASSWORD
        sync: false

      - key: SEED_SUPERADMIN_FIRSTNAME
        value: Super

      - key: SEED_SUPERADMIN_LASTNAME
        value: Admin

      - key: RATE_LIMIT_AUTH
        value: "20"

      - key: RATE_LIMIT_DEFAULT
        value: "500"
